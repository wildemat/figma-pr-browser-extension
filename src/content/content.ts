// Content script for the browser extension
// This will be the entry point for the content script functionality

console.log('Figma PR Extension content script loaded')

// Import the existing functionality from the shared directory
// We'll gradually migrate this to React components